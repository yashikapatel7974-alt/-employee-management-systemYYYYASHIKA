{
  "name": "enterprise-ems-root",
  "version": "1.0.0",
  "description": "Root package for Render monorepo deployment",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
